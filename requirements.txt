fastapi>=0.111.0
uvicorn>=0.30.1
sqlalchemy>=2.0.30
# Required at runtime by SQLAlchemy's async engine (AsyncSession/greenlet_spawn).
# Not always pulled in transitively depending on platform/pip resolver, so it's
# pinned explicitly here to avoid "the greenlet library is required" errors.
greenlet>=3.0.0
alembic>=1.13.1
# Powers the real-time WebSocket features (app/api/socket.py, main.py's
# `import socketio`). Not a transitive dep of anything else here, so it
# must be listed explicitly or a fresh install throws
# "ModuleNotFoundError: No module named 'socketio'".
python-socketio[asyncio_client]>=5.11.0
psycopg2-binary>=2.9.9
asyncpg>=0.29.0
pydantic>=2.7.4
pydantic-settings>=2.3.4
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
apscheduler>=3.10.4
boto3>=1.34.0
faiss-cpu>=1.7.4
email-validator>=2.1.0
Pillow>=10.0.0
aiosqlite>=0.20.0

# ML & Data Pipeline dependencies
datasets>=2.19.0
pandas>=2.2.0
scikit-learn>=1.4.0
imbalanced-learn>=0.12.0
celery>=5.3.6
redis>=5.0.1  

# RAG dependencies
groq>=0.30.0
google-genai>=0.3.0
numpy>=1.26.0
python-dotenv>=1.0.1
sentence-transformers>=3.0.1
transformers>=4.53.0
torch>=2.7.0

# PDF report generation (app/services/reports/pdf_generator.py), imported
# eagerly by app/api/routes/reports.py at startup — missing this crashes
# uvicorn on boot, not just when the reports endpoint is hit.
reportlab>=4.0.0

# Model training/evaluation scripts (app/ml/train.py, app/ml/evaluate.py)
xgboost>=2.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
